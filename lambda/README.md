Supporting files for running an Amanzi API instance on an AWS Lambda function.
The lambda function is triggered by an API Gateway endpoint.

Build using:

```
docker build --platform linux/amd64 --provenance=false -t amanzi-lambda:test .
```
